/**
 * @format
 */

import {AppRegistry} from 'react-native';
import AnimatedStyleUpdateExample from './App';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => AnimatedStyleUpdateExample);
